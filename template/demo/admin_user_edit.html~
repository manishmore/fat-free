<F3:check if="{{ @messages }}">
    <p><span class="fail">{{ @messages }}</span></p>
</F3:check>
<label for="name">Username*</label>
        <F3:check if="{{@edit}}==1">
            <F3:true>
                <span id="name">{{@POST.name}}</span>
            </F3:true>
            <F3:false>
                <input id="name" name="name" type="text" value="{{@POST.name}}" required/>
            </F3:false>
        </F3:check>
<label for="titel">Email:*</label>
<input id="titel" name="email" type="text" value="{{@POST.email}}" required/>
 <span class="msg error" style="display: none;">Not a valid email address</span>

<label for="titel">Organization:</label>
<input id="titel" name="organization" type="text" value="{{@POST.organization}}">

<F3:check if="{{@edit}}==1">
	<F3:true>	
	<label for="titel">Password:</label>
	<input id="titel" name="pw" type="text" value="{{@POST.pw}}" >
	
	</F3:true>
	
	<F3:false>
	
	</F3:false>
</F3:check>


